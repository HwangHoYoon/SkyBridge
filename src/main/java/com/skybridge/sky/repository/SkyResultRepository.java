package com.skybridge.sky.repository;

import com.skybridge.sky.entity.SkyResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SkyResultRepository extends JpaRepository<SkyResult, Long> {
}